<h1>All Projects</h1>
  <% @projects.each do |project| %>
  <h3><%= link_to project.name, project_path(project) %></h3>
  <h5>Funding goal: $<%= project.funding_goal %></h5>
  <p><%= project.description %></p>
<% end %>
